# 洋流 (Ocean Stream) - 未完成计划 (To-Do)

这里记录了接下来需要推进的核心任务与待开发功能。

## 1. 画布交互增强 (Frontend)
* [ ] **节点状态持久化**: 当前刷新后节点会重置，需要接入本地存储，记住用户拖拽后的节点位置。
* [ ] **动态添加节点**: 在界面侧边栏增加“工具箱”，允许用户将新的设备或服务拖入画布。
* [ ] **节点参数配置面板**: 点击画布中的节点时，右侧弹出抽屉，允许配置该节点的 IP、端口、Token 等参数。

## 2. 核心通信与逻辑 (Backend / Rust)
* [ ] **MCP 协议客户端集成**: 让客户端能够真正与本地或远程的 MCP (Model Context Protocol) Server 建立连接。
* [ ] **与 Ruflo Queen 的握手**: 编写网络层代码，使客户端能够向协调节点发送注册和心跳请求。
* [ ] **本地终端指令执行**: 通过 Tauri 的 Command API，允许 UI 界面直接下发命令到系统的终端中运行。

## 3. UI/UX 细节打磨
* [ ] **节点实时状态指示**: 根据真实网络连通性，改变节点的颜色（例如掉线变灰/红，正常为绿）。
* [ ] **系统托盘 (Tray)**: 增加 Windows/macOS 的系统右下角托盘图标，支持后台静默运行。
* [ ] **应用图标定制**: 替换掉目前 Tauri 默认的放大镜 Logo，设计并使用 Ocean Stream 的专属图标。